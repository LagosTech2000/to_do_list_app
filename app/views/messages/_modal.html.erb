<div id="modal" class="fixed inset-0 z-50 overflow-auto bg-black bg-opacity-50 flex items-center justify-center">
  <div class="relative bg-white rounded-lg p-8">
    <div class="absolute top-0 right-0 p-2">
      <button onclick="closeModal()" class="bg-dark text-gray-400 hover:text-gray-600 focus:outline-none">
        <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
      </button>
    </div>
    <div id="chats" class="overflow-y-auto max-h-64">      
      <%= render 'messages/message', collection: conversation.messages %>
      asdas
    </div>
  </div>
</div>

<script>
  function closeModal() {
    document.getElementById('modal').remove();
  }
</script>
